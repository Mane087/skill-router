---
name: foreign-fields
description: Carries frontmatter fields written by other tools that share this file.
allowed-tools:
  - Read
  - Grep
license: MIT
metadata:
  author: somebody-else
framework: angular
---

# Foreign fields

`allowed-tools`, `license` and `metadata` belong to other tools. `framework` is
a misspelling of `frameworks`, and it is reported for exactly that reason.
